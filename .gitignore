# Toolchain build outputs: added at M1 by the toolchain ADR

# Large data never committed; fetch scripts + checksum manifests live in tools/
tools/data/

# Editor / OS noise
*.swp
.DS_Store

# Simulation build products
build/
*.vcd

site/.suite.log
__pycache__/

# The site is built by CI from a live suite run, never committed stale
site/public/
